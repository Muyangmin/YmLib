apply plugin: 'java-library'
apply from: "../common-config.gradle"

dependencies {
    implementation deps.javapoet
    implementation project(":ymlib-annotation")
}

publish {
    artifactId = "ymlib-compiler"
}